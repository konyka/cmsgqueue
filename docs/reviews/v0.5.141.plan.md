# v0.5.141 — attach JWKS refresh on reload

## Goal

SIGHUP starts the JWKS sidecar when create had a cache
but no refresher. No GET in reload.

## Performance

One parse + thread create when starting. Existing sidecar
is one pointer check.

## Security

Bad scheme / host / path and out-of-range interval fail
closed. Empty/0 cannot start a half-built sidecar.

## Stop

- Unit tests green.
- Push `v0.5.141` to `origin`.
