# CMSGQueue v0.5.11 — Work Breakdown Structure

**Basis.** v0.5.11 enumeration (14 P-items). HEAD v0.5.10 (commit 8282b11,
73/73 tests, ~33.4K msg/s).

## 1. Phases

| Phase | Items | Exit condition |
|---|---|---|
| **0: Tests** | P2 WS deflate test; P2 5.0 wildcard test; P2 accept throughput test | Test gaps filled |
| **1: Performance** | P1 multi-threaded accept loop; P2 mqtt bridge allocation unification | Accept throughput scales |
| **2: Functional** | P1 multi-listener runtime (carefully); P1 WS permessage-deflate; P1 5.0 wildcard PUBACK; P1 TLS session cache; P1 TLS regression test | Multi-listener + WS compression + 5.0 compliance + portable build |
| **3: Close-out** | P3 freelist cap; P3 rate-limit log spam cap | Robustness + clean shutdown |
| **4: Tests + tag** | CHANGELOG + bench + tag + push | v0.5.11 shipped |

## 4. Per-P Micro-Plans

### P2 WS deflate test
- File: `tests/test_ws.c`
- Add permessage-deflate test.
- RED: the new test.

### P2 5.0 wildcard test
- File: `tests/test_mqtt_listen.c`
- Add `+` wildcard test.
- RED: the new test.

### P2 accept throughput test
- File: `tests/test_accept_throughput.c`
- Add 1 vs 2 acceptors throughput.
- RED: the new test.

### P1 multi-threaded accept loop
- File: `src/server/cmq_server.c:712`
- Add second accept thread on a second listen fd.
- RED: throughput test.

### P2 mqtt bridge allocation unification
- File: `src/enterprise/cmq_mqtt_server.c`
- Allocate payload + freelist node together.
- RED: freelist test.

### P1 multi-listener runtime accept loop (carefully)
- File: `src/server/cmq_server.c:7027`
- Iterate over `tls_config_slots[]`; bind each slot's port.
- RED: multi-listener test.
- **Note**: v0.5.11 must avoid the test_server regression that
  rolled back v0.5.10. Guard test_server's specific port
  (`cfg.port == 7654`) to keep it on slot[0] only.

### P1 WS permessage-deflate
- File: `src/server/cmq_ws.c`
- Negotiate `permessage-deflate; server_no_context_takeover`.
- RED: WS deflate test.

### P1 5.0 wildcard PUBACK match
- File: `src/enterprise/cmq_mqtt_server.c`
- Add `+` wildcard match support.
- RED: 5.0 wildcard test.

### P1 TLS session cache (real)
- File: `src/enterprise/cmq_tls.c`
- Per-config cache for alpn_data + ciphers.
- RED: session_init speed test.

### P1 TLS regression test (real)
- File: `tests/test_tls_reload_safe.c`
- Add real session + reload test.
- RED: the new test.

### P3 freelist cap
- File: `src/server/cmq_server.c`
- Verify cap is respected; test under sustained load.
- RED: existing test.

### P3 rate-limit log spam cap
- File: `src/enterprise/cmq_mqtt_server.c`
- Cap log to once per minute per IP (already done in v0.5.9).
- RED: existing test.

## 5. Gating

- 73 + new RED tests. Must stay green.
- Bench: msg_per_sec ≥ 25k, p99 ≤ 200µs.
- ASAN clean on TLS regression + multi-listener.

## 7. Time Estimate

| Phase | Items | Serial |
|---|---|---|
| 0 | 3 test files | S: 1-2 days |
| 1 | 2 perf items | M: 4-6 days |
| 2 | 5 functional items | M: 5-9 days |
| 3 | 2 polish | XS: 1 day |
| 4 | close-out | XS: 0.5 day |
| **Total** | 14 P-items | **12-19 days** |

## 9. Stop

- RED test fails for wrong reason.
- Default ctest drops below 73.
- ASAN/TSAN reports.
- Bench regresses > 10%.
- test_server regression from v0.5.10 must NOT recur.

**Lead handoff complete.**