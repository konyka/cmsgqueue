# v0.5.129 — audit file from persist_dir

## Goal

A server with `persist_dir` writes structured audit events to
`{persist_dir}/cmq-audit.log`.

## Performance

One path store at create / reload. Event writes are unchanged
(stderr + optional fopen append).

## Security

`..` / `\` / controls fail closed. Destroy clears the global
path. Reload omitted/empty cannot wipe a working file.

## Stop

- Unit tests green (new + existing audit).
- Push `v0.5.129` to `origin`.
