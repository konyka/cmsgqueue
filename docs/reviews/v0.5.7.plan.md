# CMSGQueue v0.5.7 — Work Breakdown Structure

**Basis.** v0.5.7 enumeration (15 P-items). HEAD v0.5.6 (commit 96cc556, 71/71 tests, ~32K msg/s).

## 1. Phases

| Phase | Items | Exit condition |
|---|---|---|
| **0: Safety fixes** | P1 close-by-fd; P1 mqtt_bridge_shutdown wired; P1 TLS partial cleanup; P2 accept fd leak; P3 mqtt listener log; P3 freelist cap | Bad config + shutdown paths don't leak |
| **1: Performance** | P1 TLS session_init cache; P1 multi-threaded accept loop | TLS handshake latency reduced; accept throughput doubled |
| **2: Functional** | P1 multi-listener runtime; P1 WS permessage-deflate; P2 5.0 wildcard PUBACK; P2 cmq_atomic_u64 32-bit; P3 mqtt bridge freelist; P3 TLS regression test | 5.0 client compatibility; 32-bit build; less malloc |
| **3: Close-out** | CHANGELOG + bench + tag + push | v0.5.7 shipped |

## 4. Per-P Micro-Plans

### P1 close-by-fd protection
- File: `src/server/cmq_server.c`
- Verify cmq_clients[] uses fd+gen; write test.
- RED: `tests/test_accept_race.c::close_then_rebind_safe`.

### P1 mqtt_bridge_shutdown wired
- File: `src/server/cmq_server.c` (cmq_server_destroy)
- Call cmq_mqtt_bridge_shutdown() before destroying the sublist.
- RED: `tests/test_mqtt_bridge.c::shutdown_drains_in_flight`.

### P1 TLS partial-state cleanup
- File: `src/enterprise/cmq_tls.c:331`
- Add `goto fail` cleanup on each early-out path.
- RED: `tests/test_tls_api.c::bad_config_no_leak`.

### P1 TLS session_init cache
- File: `src/enterprise/cmq_tls.c`
- Cache common config results in cmq_tls_config_t; cache-hit fast path.
- RED: `tests/test_tls_session_init.c::second_init_faster`.

### P1 Multi-threaded accept loop
- File: `src/server/cmq_server.c:712`
- Add a second accept thread on a second listen fd.
- RED: `tests/test_accept_throughput.c::two_acceptors_double_rate`.

### P1 Multi-listener runtime accept loop
- File: `src/server/cmq_server.c:7027`
- Iterate over `tls_config_slots[]`; bind each slot's port.
- RED: `tests/test_p5_listener.c::two_listeners_distinct_ctx`.

### P1 WS permessage-deflate
- File: `src/server/cmq_ws.c`
- Negotiate `permessage-deflate; server_no_context_takeover`.
- RED: `tests/test_ws.c::permessage_deflate_compresses`.

### P2 cmq_atomic_u64 32-bit portability
- File: `src/core/cmq_atomic.h`
- static_assert + fallback to cmq_atomic_u32 split under -m32.
- RED: `tests/test_atomic_32.c::atomic_64_works`.

### P2 5.0 wildcard PUBACK
- File: `src/enterprise/cmq_mqtt_server.c`
- Add `+` wildcard match support.
- RED: `tests/test_mqtt_listen.c::wildcard_subscribe_match`.

### P2 accept fd leak on shutdown
- File: `src/server/cmq_server.c`
- Close all listen_fds in cmq_server_stop.
- RED: `tests/test_listen_close.c::shutdown_closes_fds`.

### P3 mqtt bridge freelist
- File: `src/enterprise/cmq_mqtt_server.c`
- Per-bridge freelist for payload buffers.
- RED: `tests/test_mqtt_bridge_freelist.c::no_malloc_under_loop`.

### P3 freelist growth cap
- File: `src/server/cmq_server.c`
- Per-worker msg_freelist cap; free excess to heap.
- RED: `tests/test_freelist_cap.c::excess_freed`.

### P3 mqtt listener log
- File: `src/enterprise/cmq_mqtt_server.c`
- Log on graceful exit.
- RED: `tests/test_mqtt_listen_log.c::exit_logged`.

### P3 TLS regression test
- File: `src/enterprise/cmq_tls.c`
- Test that reload during session doesn't UAF.
- RED: `tests/test_tls_reload_safe.c::no_uaf`.

## 5. Gating

- 71 + new RED tests. Must stay green.
- Bench: msg_per_sec ≥ 25k, p99 ≤ 200µs.
- ASAN clean on TLS session cache + close-by-fd + freelist.
- TSAN clean on multi-threaded accept.

## 7. Time Estimate

| Phase | Items | Serial |
|---|---|---|
| 0 | 6 small safety/polish | S: 2-3 days |
| 1 | TLS cache (M) + multi-accept (L) | L: 5-8 days |
| 2 | multi-listener (M) + WS deflate (L) + 5.0 wildcard (M) + atomic (XS) + bridge freelist (S) + TLS test (S) | M: 5-8 days |
| 3 | CHANGELOG + tag | XS: 0.5 day |
| **Total** | 15 P-items | **13-20 days** |

## 9. Stop

- RED test fails for wrong reason.
- Default ctest drops below 71.
- ASAN/TSAN reports.
- Bench regresses > 10%.
- TLS cache or multi-accept regresses single-conn throughput.

**Lead handoff complete.**