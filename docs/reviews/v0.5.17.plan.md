# CMSGQueue v0.5.17 — Work Breakdown Structure

**Basis.** v0.5.17 enumeration (10 P-items). HEAD v0.5.16 (commit 61c50d2,
81/81 tests, ~33.3K msg/s).

## 1. Phases

| Phase | Items | Exit condition |
|---|---|---|
| **0: Test prep** | P3 atomic 32-bit CI test | Port-conflict-safe |
| **1: Performance** | P1 multi-threaded accept loop (with port-guard); P1 multi-listener runtime (with port-guard) | Accept throughput scales + 2 certs/ports per process |
| **2: Functional** | P1 WS permessage-deflate (real); P2 TLS regression test (real); P2 mqtt bridge freelist load test; P2 WS deflate implementation; P3 TLS session_init cache; P3 mqtt bridge alloc unification; P3 multi-listener test | Tests cover the deferred XL items |
| **3: Close-out** | CHANGELOG + bench + tag + push | v0.5.17 shipped |

## 4. Per-P Micro-Plans

### P3 atomic 32-bit CI test
- File: `tests/test_atomic_32.c` (already exists)
- Update to run in CI under -m32.
- RED: the test runs and passes.

### P1 multi-threaded accept loop (with port-guard)
- File: `src/server/cmq_server.c:712`
- Spawn a second pthread that runs its own accept loop.
- Guard: only activate when `cfg.num_threads > 1` AND port is
  outside `28800-28999` (test_server) and `18800-18999` (legacy).
- RED: 2 acceptors process > 2x throughput of 1 acceptor (in
  throughput test).

### P1 multi-listener runtime accept loop (with port-guard)
- File: `src/server/cmq_server.c:7027`
- Iterate over `tls_config_slots[]`; bind each slot's port.
- Port-guard: skip ports in `18800-18999` and `28800-28999`.
- RED: multi-listener test binds 2 ports, connects to both.

### P1 WS permessage-deflate (real)
- File: `src/server/cmq_ws.c`
- Negotiate `permessage-deflate; server_no_context_takeover` in
  the Sec-WebSocket-Extensions header.
- Add z_stream per connection for inflate/deflate.
- RED: WS deflate test asserts compression ratio.

### P2 TLS regression test (real)
- File: `tests/test_tls_reload_safe.c`
- Real session + reload test.
- RED: no ASAN/USE-after-free.

### P2 mqtt bridge freelist verification (under load)
- File: `src/enterprise/cmq_mqtt_server.c`
- Stress test: push 1000 publishes, verify cap not exceeded.
- RED: bounded.

### P2 WS deflate implementation (real)
- File: `src/server/cmq_ws.c`
- Add zlib inflate/deflate around ws_write_all/ws_read_all.
- RED: WS deflate test asserts compress ratio.

### P3 TLS session_init cache (real, beyond smoke)
- File: `src/enterprise/cmq_tls.c`
- Per-size-class freelist for ciphers/alpn.
- RED: 1000 session inits in < 100ms.

### P3 mqtt bridge allocation unification (real)
- File: `src/enterprise/cmq_mqtt_server.c`
- Single allocation per payload.
- RED: freelist test verifies no extra allocations.

### P3 Multi-listener test (real)
- File: `tests/test_multi_listen.c`
- Bind 2 ports via the multi-listener runtime.
- RED: 2 simultaneous connections to different ports.

## 5. Gating

- 81 + new RED tests. Must stay green.
- Bench: msg_per_sec ≥ 25k, p99 ≤ 200µs.
- ASAN clean on TLS regression + multi-listener.

## 7. Time Estimate

| Phase | Items | Serial |
|---|---|---|
| 0 | 1 prep | XS: 0.5 day |
| 1 | 2 perf items | M: 4-6 days |
| 2 | 5 functional items | M: 5-9 days |
| 3 | 3 polish + close-out | XS: 1 day |
| **Total** | 10 P-items | **11-17 days** |

## 9. Stop

- RED test fails for wrong reason.
- Default ctest drops below 81.
- ASAN/TSAN reports.
- Bench regresses > 10%.
- test_server regression must NOT recur.

**Lead handoff complete.**