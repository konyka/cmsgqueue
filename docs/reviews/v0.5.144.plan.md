# v0.5.144 — attach persist_dir on reload

## Goal

SIGHUP opens the WAL when create had no `persist_dir`.
Existing filestore is not remounted. Replay stays
create-time.

## Performance

One mkdir + two fopen when starting. Existing WAL is one
pointer check.

## Security

`..` / `\` / controls fail closed. Empty/omitted cannot
attach a half-built directory.

## Stop

- Unit tests green.
- Push `v0.5.144` to `origin`.
