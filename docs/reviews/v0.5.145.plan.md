# v0.5.145 — fetch JWKS on reload

## Goal

SIGHUP GETs `jwks_url` when create had no cache.
Existing cache is not replaced.

## Performance

One HTTP GET when starting a cache. Existing cache is one
pointer check.

## Security

Bad scheme / host / path fail closed. Failed GET does not
install an empty cache.

## Stop

- Unit tests green (including loopback apply).
- Push `v0.5.145` to `origin`.
