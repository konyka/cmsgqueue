# v0.5.64 — OTLP/HTTP JSON exporter

## Goal

A configured `otlp_endpoint` receives OTLP/JSON POSTs of
sidecar-drained spans.

## Performance

Off the publish offer path. Default servers: no hook.

## Security

Plain HTTP only. URL from config, not from span data.
Header-safe host/path alphabet.

## Stop

- Unit tests green (new + otel).
- Push `v0.5.64` to `origin`.
