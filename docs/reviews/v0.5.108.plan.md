# v0.5.108 — `$JS` history WAL rotate

## Goal

Bound `{name}.msgs` so durable `$JS` history cannot grow
past a configured byte cap.

## Performance

`cap=0`: one compare after append. Compact scans only
when the file is already at the cap; the keep window is
1024 offsets.

## Security

Path rules unchanged. Compact copies complete `CMQM`
records only; a bad header fails closed (live file kept).

## Stop

- Unit tests green (new + existing js / jsh / jsd).
- Push `v0.5.108` to `origin`.
