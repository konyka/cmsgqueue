# v0.5.149 — attach blocklist on reload

## Goal

SIGHUP loads `blocklist_file` when create had none.
Existing handles are not remounted.

## Performance

One file read per attach. Existing handle is one pointer
compare, then the existing swap path.

## Security

`..` / `\` / missing file fail closed. Empty/omitted
cannot drop a working list.

## Stop

- Unit tests green (including file apply).
- Push `v0.5.149` to `origin`.
