# v0.5.143 — attach cluster on reload

## Goal

SIGHUP starts cluster identity when create had none.
Route peers stay create-time.

## Performance

One calloc + mutex init when starting. Existing cluster
is one pointer check.

## Security

Oversize name/id fail closed. Empty/omitted cannot
attach a half-built cluster.

## Stop

- Unit tests green.
- Push `v0.5.143` to `origin`.
