# v0.5.84 — OTLP/gRPC

## Goal

A `grpc://` endpoint exports the same span as an OTLP
gRPC Export over HTTP/2.

## Performance

Encode and I/O stay on the sidecar. Offer is unchanged.

## Security

Same host/path checks. No userinfo. Caps unchanged.

## Stop

- Unit tests green (new + existing OTLP).
- Push `v0.5.84` to `origin`.
