# v0.5.70 — TLS 1.2 client negotiates down defensive test

## Scope

Complements the v0.5.53 `tls13_negotiated` test. A TLS 1.2-only
client connects to a default `cmq_server` (no max-version cap
→ would prefer TLS 1.3). OpenSSL's protocol negotiation finds
the highest common version (TLS 1.2). The handshake succeeds
at TLS 1.2; the test asserts the NEGOTIATED version is 0x0303
(TLS 1.2), not 0x0304 (TLS 1.3).

This guards against a future "default to TLS 1.3 only" change
that would silently break compatibility with TLS 1.2 clients.

## Test: `tls_e2e_handshake.tls12_client_negotiates_down`

1. Generate a server cert.
2. Configure `cmq_server` with `tls_enabled=1`, no
   `tls_verify_peer` (default version negotiation).
3. Connect with a TLS 1.2-only client
   (`min=max=TLS1_2_VERSION`).
4. Assert the handshake succeeds AND the negotiated version
   is 0x0303 (TLS 1.2).

## What v0.5.70 ships

Just the test. No production change.

## Verified

- `tests/test_tls_e2e_handshake.c::tls12_client_negotiates_down` PASS
  (5/5 runs).
- Full ctest: 108/108 pass.
- Bench: ~33K msg/s, p99 99 µs (unchanged).

## Deferred to v0.5.71+

- TLS 1.3 mTLS via post-handshake auth (v0.5.48/v0.5.49 race).
- More defensive tests (TLS fragment reassembly, etc.).
- Per-listener `accept_thread_func` refactor (already on remote
  workstream as v0.5.42).