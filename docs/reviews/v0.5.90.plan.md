# v0.5.90 — JWT HS256 issuing

## Goal

Mint a compact HS256 JWT that `cmq_jwt_verify_hs256`
accepts for CONNECT.

## Performance

Sign is an HMAC of a small header+payload. Not on PUBLISH.

## Security

iss/sub cannot carry JSON metacharacters. Empty secret
and missing exp fail closed.

## Stop

- Unit tests green (new + existing jwt).
- Push `v0.5.90` to `origin`.
