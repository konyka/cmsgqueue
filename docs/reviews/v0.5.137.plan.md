# v0.5.137 — reload jwks_url

## Goal

SIGHUP applies `jwks_url` to a running JWKS sidecar.
The next refresh uses the new host. First GET stays
create-time.

## Performance

One mutex + struct copy. GET cadence unchanged.

## Security

Bad scheme / host / path (`..`) fail closed. Empty/omitted
cannot wipe a working URL. Snapshot under lock avoids a
torn host mid-GET. CA is preserved across URL apply.

## Stop

- Unit tests green.
- Push `v0.5.137` to `origin`.
