# CMSGQueue v0.5.9 — Work Breakdown Structure

**Basis.** v0.5.9 enumeration (14 P-items). HEAD v0.5.8 (commit 779a17d,
73/73 tests, ~33.2K msg/s).

## 1. Phases

| Phase | Items | Exit condition |
|---|---|---|
| **0: Tests** | P2 multi-listener test; P2 WS deflate test; P2 5.0 wildcard test; P2 atomic 32-bit test; P2 accept throughput test | Test gaps filled |
| **1: Performance** | P1 multi-threaded accept loop; P3 mqtt bridge allocation unification | Accept throughput scales |
| **2: Functional** | P1 multi-listener runtime; P1 WS permessage-deflate; P1 5.0 wildcard PUBACK; P1 cmq_atomic_u64 32-bit; P1 TLS session cache; P3 TLS regression test (real) | Multi-listener + WS compression + 5.0 compliance + portable build |
| **3: Close-out** | CHANGELOG + bench + tag + push | v0.5.9 shipped |

## 4. Per-P Micro-Plans

### P2 multi-listener test
- File: `tests/test_p5_listener.c`
- Add second-listener test.
- RED: the new test.

### P2 WS deflate test
- File: `tests/test_ws.c`
- Add permessage-deflate test.
- RED: the new test.

### P2 5.0 wildcard test
- File: `tests/test_mqtt_listen.c`
- Add `+` wildcard test.
- RED: the new test.

### P2 atomic 32-bit test
- File: `tests/test_atomic_32.c`
- Add atomic_64-on-32 test.
- RED: the new test.

### P2 accept throughput test
- File: `tests/test_accept_throughput.c`
- Add 1 vs 2 acceptors throughput.
- RED: the new test.

### P1 multi-threaded accept loop
- File: `src/server/cmq_server.c:712`
- Add second accept thread on a second listen fd.
- RED: throughput test.

### P3 mqtt bridge: payload + freelist in one allocation
- File: `src/enterprise/cmq_mqtt_server.c`
- Allocate payload + freelist node together.
- RED: freelist test.

### P1 multi-listener runtime accept loop
- File: `src/server/cmq_server.c:7027`
- Iterate over `tls_config_slots[]`; bind each slot's port.
- RED: multi-listener test.

### P1 WS permessage-deflate
- File: `src/server/cmq_ws.c`
- Negotiate `permessage-deflate; server_no_context_takeover`.
- RED: WS deflate test.

### P1 5.0 wildcard PUBACK match
- File: `src/enterprise/cmq_mqtt_server.c`
- Add `+` wildcard match support.
- RED: 5.0 wildcard test.

### P1 cmq_atomic_u64 32-bit portability
- File: `src/core/cmq_atomic.h`
- static_assert + fallback to cmq_atomic_u32 split.
- RED: atomic 32-bit test.

### P1 TLS session cache (real)
- File: `src/enterprise/cmq_tls.c`
- Per-config cache for alpn_data + ciphers.
- RED: session_init speed test.

### P3 TLS regression test (real)
- File: `tests/test_tls_reload_safe.c`
- Add real session + reload test.
- RED: the new test.

## 5. Gating

- 73 + new RED tests. Must stay green.
- Bench: msg_per_sec ≥ 25k, p99 ≤ 200µs.
- ASAN clean on TLS regression + multi-listener.

## 7. Time Estimate

| Phase | Items | Serial |
|---|---|---|
| 0 | 5 test files | S: 1-2 days |
| 1 | 2 perf items | M: 4-6 days |
| 2 | 6 functional items | M: 5-9 days |
| 3 | close-out | XS: 0.5 day |
| **Total** | 14 P-items | **11-18 days** |

## 9. Stop

- RED test fails for wrong reason.
- Default ctest drops below 73.
- ASAN/TSAN reports.
- Bench regresses > 10%.

**Lead handoff complete.**