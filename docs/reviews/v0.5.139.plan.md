# v0.5.139 — attach OTLP exporter on reload

## Goal

SIGHUP starts the OTLP exporter when create had no
endpoint. Next span uses the new URL. No POST in reload.

## Performance

One parse + calloc. Export cadence unchanged until a span.

## Security

Bad scheme / host / path (`..`) fail closed. Empty/omitted
cannot attach a half-built URL.

## Stop

- Unit tests green.
- Push `v0.5.139` to `origin`.
