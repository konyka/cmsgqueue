# v0.5.130 — reload $JS partitions / rotate

## Goal

SIGHUP applies `js_partitions` and `js_msgs_rotate_bytes`
to the live `$JS` table. New streams inherit the new
default. Existing `.parts` files stay.

## Performance

Two integer stores under the JS mutex. No hot-path change.

## Security

Out-of-range fails closed. 0 / omitted cannot wipe a
working default or rotate cap.

## Stop

- Unit tests green (new + existing $JS).
- Push `v0.5.130` to `origin`.
