# v0.5.111 — MQTT bridge config-file keys

## Goal

Operators can enable the outbound MQTT bridge from
`cmq.conf` without a programmatic assignment.

## Performance

Two extra `strcmp`s per config line. Create path gains one
`strdup` when the addr is set.

## Security

`/` `\` controls and spaces fail closed. Port above 65535
fails closed.

## Stop

- Unit tests green (new + config / pdir / psyn).
- Push `v0.5.111` to `origin`.
