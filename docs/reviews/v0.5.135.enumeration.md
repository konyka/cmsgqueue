# v0.5.135 — reload otlp_ca

## Scope
Create copies `otlp_ca` onto the OTLP URL. Reload ignored
the path, so SIGHUP could not point HTTPS verify at a
new PEM.

## Design (performance-first, safety, reliability)

- `cmq_otlp_reload_ca`: empty/omitted keeps.
  `cmq_otlp_set_ca` rules (`..`, length) fail closed.
- Does not change `otlp_endpoint` or start an exporter.

## TDD
1. **Red**: apply, omitted, empty, reject.
2. **Green**: helper + `cmq_server_reload` call.
3. **Verify**: existing OTLP tests still compile.

## Files
- `src/server/cmq_otlp.{h,c}`
- `src/server/cmq_server.c`
- `tests/test_oca.c`
- `docs/features/otel.md`
- CHANGELOG, this file, plan, benches
