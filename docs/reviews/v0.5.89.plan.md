# v0.5.89 — Consume spans

## Goal

A successful local fanout queues `KIND_CONSUME` with the
publisher trace id.

## Performance

One extra offer after fanout success. Zero-subscriber
publishes do not touch the ring again.

## Security

No subject or payload on the span (same as publish).

## Stop

- Unit tests green (new + existing otel).
- Push `v0.5.89` to `origin`.
