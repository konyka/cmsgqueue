# v0.5.115 — extra-listener bind host/port

## Goal

Extra listeners bind the configured IPv4 host/port when
set; omitted values keep `127.0.0.1:port+li`.

## Performance

One extra `strdup` per configured extra-listener host at
create. Bind loop already ran for `listener_count > 1`.

## Security

Non-IPv4 hosts fail closed at load. Port outside 0–65535
fails closed. Existing test-port guards still skip the
extra-bind branch.

## Stop

- Unit tests green (new + lstn / config).
- Push `v0.5.115` to `origin`.
