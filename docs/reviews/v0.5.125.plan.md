# v0.5.125 — reload account_max_* defaults

## Goal

SIGHUP updates the manager defaults that new accounts
inherit, without walking live account slots.

## Performance

Off the PUBLISH path. Four atomic stores per reload.

## Security

Omitted 0 does not disable a working default.
Existing connections keep CONNECT-cached caps.

## Stop

- Unit tests green (new + account_limits).
- Push `v0.5.125` to `origin`.
