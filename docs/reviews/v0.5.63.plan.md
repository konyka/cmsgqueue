# v0.5.63 — NKEY on CONNECT

## Goal

A client that signs `CMQNK1|<user>` with the configured
Ed25519 public key is admitted on CONNECT.

## Performance

Off accept. Default servers: one NULL compare.

## Security

Verify-only. Hex alphabet only. Empty user rejected.
JWT and nkey are not mixed on one CONNECT.

## Stop

- Unit tests green (new + jwt).
- Push `v0.5.63` to `origin`.
