# v0.5.135 — reload otlp_ca

## Goal

SIGHUP applies `otlp_ca` to the live OTLP exporter URL.
The next export uses the new PEM. Endpoint stays
create-time.

## Performance

One memcpy of a path. Export path unchanged.

## Security

`..` fails closed. Empty/omitted cannot wipe a working
CA.

## Stop

- Unit tests green.
- Push `v0.5.135` to `origin`.
