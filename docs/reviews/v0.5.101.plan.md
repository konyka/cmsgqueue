# v0.5.101 — Response spans

## Goal

A successful local RESPONSE deliver queues `KIND_RESPONSE`
with the accept trace id.

## Performance

One extra offer on the RESPONSE success path only.
Failed / route-only RESPONSE does not touch the ring.

## Security

No subject, inbox, or payload on the span.

## Stop

- Unit tests green (new + existing otel).
- Push `v0.5.101` to `origin`.
