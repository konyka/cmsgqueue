# CMSGQueue v0.5.8 — Work Breakdown Structure

**Basis.** v0.5.8 enumeration (17 P-items). HEAD v0.5.7 (commit eb7fec2,
72/72 tests, ~33.7K msg/s).

## 1. Phases

| Phase | Items | Exit condition |
|---|---|---|
| **0: Safety + cleanup** | P2 accept fd double-close; P2 mqtt sub/qos2 disconnect clear; P2 rate-limit log; P2 freelist overflow | No double-close, no mqtt sub leak, ops visibility |
| **1: Performance** | P1 multi-threaded accept loop; P1 mqtt bridge freelist | Accept throughput scales; bridge payload no malloc per publish |
| **2: Functional** | P1 multi-listener runtime; P1 WS permessage-deflate; P1 5.0 wildcard PUBACK; P1 cmq_atomic_u64 32-bit; P2 TLS session cache; P1 TLS regression test | Multi-listener + WS compression + 5.0 compliance + portable build |
| **3: Tests + close-out** | P3 test files; CHANGELOG + bench + tag + push | v0.5.8 shipped |

## 4. Per-P Micro-Plans

### P2 accept fd double-close guard
- File: `src/server/cmq_server.c`
- Skip double-close in cmq_server_destroy.
- RED: existing tests don't break.

### P2 mqtt sub/qos2 disconnect clear
- File: `src/enterprise/cmq_mqtt_server.c`
- Clear g_mqtt_sub_topics + g_qos2 on disconnect (currently only CONNECT).
- RED: `tests/test_mqtt_listen.c::disconnect_clears_sub_topics`.

### P2 rate-limit log
- File: `src/enterprise/cmq_mqtt_server.c`
- Add cmq_log_info on first rate-limit hit per source IP.
- RED: smoke test.

### P2 freelist overflow path
- File: `src/server/cmq_server.c`
- Verify cap is respected; test under ASAN.
- RED: `tests/test_freelist_cap.c`.

### P1 multi-threaded accept loop
- File: `src/server/cmq_server.c:712`
- Add a second accept thread on a second listen fd.
- RED: `tests/test_accept_throughput.c::two_acceptors_double_rate`.

### P1 mqtt bridge freelist
- File: `src/enterprise/cmq_mqtt_server.c`
- Per-bridge freelist for payload buffers.
- RED: `tests/test_mqtt_bridge_freelist.c::no_malloc_under_loop`.

### P1 multi-listener runtime accept loop
- File: `src/server/cmq_server.c:7027`
- Iterate over `tls_config_slots[]`; bind each slot's port.
- RED: `tests/test_p5_listener.c::two_listeners_distinct_ctx`.

### P1 WS permessage-deflate
- File: `src/server/cmq_ws.c`
- Negotiate `permessage-deflate; server_no_context_takeover`.
- RED: `tests/test_ws.c::permessage_deflate_compresses`.

### P1 5.0 wildcard PUBACK match
- File: `src/enterprise/cmq_mqtt_server.c`
- Add `+` wildcard match support.
- RED: `tests/test_mqtt_listen.c::wildcard_subscribe_match`.

### P1 cmq_atomic_u64 32-bit portability
- File: `src/core/cmq_atomic.h`
- static_assert + fallback to cmq_atomic_u32 split under -m32.
- RED: `tests/test_atomic_32.c::atomic_64_works`.

### P2 TLS session cache
- File: `src/enterprise/cmq_tls.c`
- Per-config cache for alpn_data and ciphers.
- RED: `tests/test_tls_session_init.c::second_init_faster`.

### P1 TLS regression test
- File: `tests/test_tls_reload_safe.c`
- New: start session, call reload mid-handshake, assert no ASAN.
- RED: the test itself.

## 5. Gating

- 72 + new RED tests. Must stay green.
- Bench: msg_per_sec ≥ 25k, p99 ≤ 200µs.
- ASAN clean on TLS regression + freelist overflow.

## 7. Time Estimate

| Phase | Items | Serial |
|---|---|---|
| 0 | 4 small safety/cleanup | S: 2-3 days |
| 1 | 2 perf items | M: 4-7 days |
| 2 | 5 functional + 1 verification | M: 5-8 days |
| 3 | 5 small + close-out | S: 2-3 days |
| **Total** | 17 P-items | **13-21 days** |

## 9. Stop

- RED test fails for wrong reason.
- Default ctest drops below 72.
- ASAN/TSAN reports.
- Bench regresses > 10%.
- Multi-threaded accept regresses single-conn throughput.
- Multi-listener bind fails on second listener.

**Lead handoff complete.**