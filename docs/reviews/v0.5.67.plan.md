# v0.5.67 — KV bucket PUBLISH path

## Goal

PUBLISH to `$KV.<bucket>.<key>` updates a last-value bucket.

## Performance

Default subjects: one `subject[0]=='$'` compare.

## Security

Path-safe bucket/key alphabet. Malformed `$KV.` rejected.

## Stop

- Unit tests green (new + kv).
- Push `v0.5.67` to `origin`.
