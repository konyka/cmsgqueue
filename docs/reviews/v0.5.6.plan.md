# CMSGQueue v0.5.6 — Work Breakdown Structure

**Basis.** v0.5.6 enumeration (14 P-items). HEAD v0.5.5 (commit aacc452,
71/71 tests, ~32K msg/s).

## 1. Phases

| Phase | Items | Exit condition |
|---|---|---|
| **0: Safety fixes** | P1 F19b real bridge wire-up; P1 MQTT 5.0 SUBSCRIBE props | Bridge is real, not silent; 5.0 SUBSCRIBE works |
| **1: Performance** | P1 rwlock fairness; P2 handle_publish freelist; P2 rate-limit concurrency | Publish latency stable under churn; no per-call malloc |
| **2: WS / multi-listener** | P1 WS permessage-deflate; P1 multi-listener runtime; P1 multi-threaded accept | WS clients compress; 2+ certs per process; 100K conn/sec |
| **3: Polish + close-out** | P2 cmq_atomic_u64 portability; P3 close-by-fd; P3 log spam; P3 stats; P3 TLS session; P3 5.0 PUBACK | Robustness + cleanup |

## 4. Per-P Micro-Plans

### P1 F19b real bridge wire-up
- File: `src/server/cmq_server.c` (cmq_server_create)
- After building srv->sublist, call `cmq_mqtt_register_sublist_insert(&cmq_sublist_insert, srv->sublist)`.
- RED: `tests/test_mqtt_bridge.c::bridge_thread_got_insert_call` — assert the relay calls our function pointer.

### P1 MQTT 5.0 SUBSCRIBE props
- File: `src/enterprise/cmq_mqtt_server.c:524`
- Honor `mqtt_v5_props_skip` in the SUBSCRIBE handler.
- RED: `tests/test_mqtt_listen.c::v5_subscribe_skips_properties`.

### P1 RWLOCK fairness
- File: `src/server/cmq_sublist.c` + `cmq_rwlock_t`
- Switch to `PTHREAD_RWLOCK_PREFER_WRITER_NONRECURSIVE_NP` if available; otherwise add a mutex-guarded reader-count.
- RED: `tests/test_sublist_concurrent.c::writer_does_not_starve_reader`.

### P1 Multi-threaded accept loop
- File: `src/server/cmq_server.c:712`
- Add a second accept thread on a second listen fd; round-robin to worker pool.
- RED: `tests/test_accept_throughput.c::two_acceptors_double_rate`.

### P1 Multi-listener runtime accept loop
- File: `src/server/cmq_server.c:7027`
- Iterate over `tls_config_slots[]`; bind each slot's port; per-slot accept_cb.
- RED: `tests/test_p5_listener.c::two_listeners_distinct_ctx`.

### P1 WS permessage-deflate
- File: `src/server/cmq_ws.c`
- Negotiate `permessage-deflate; server_no_context_takeover` in handshake; per-conn z_stream.
- RED: `tests/test_ws.c::permessage_deflate_compresses`.

### P2 handle_publish freelist
- File: `src/server/cmq_server.c:905`
- Per-worker `cmq_worker_msg_t` freelist; LIFO on free.
- RED: `tests/test_handle_publish.c::no_malloc_under_loop`.

### P2 rate-limit concurrency
- File: `src/enterprise/cmq_mqtt_server.c:114`
- Replace mutex with per-thread cache + atomic counter.
- RED: `tests/test_mqtt_listen.c::rate_limit_concurrent_threads`.

### P2 cmq_atomic_u64 portability
- File: `src/core/cmq_atomic.h`
- Add static_assert + fallback to cmq_atomic_u32 split under -m32.
- RED: `tests/test_atomic_32.c::atomic_64_works`.

### P3 close-by-fd
- File: `src/server/cmq_server.c`
- Verify cmq_clients[] uses fd+gen; write test.
- RED: `tests/test_accept_race.c::close_then_rebind_safe`.

### P3 log under hot path
- File: `src/enterprise/cmq_tls.c:266`
- Rate-limit log to 1/sec.
- RED: `tests/test_tls_rate_limit_log.c::log_storm_clamped`.

### P3 stats counter on worker thread
- File: `src/server/cmq_server.c:2211`
- Verify per-thread contention is acceptable.
- RED: existing bench.

### P3 TLS session_init latency
- File: `src/enterprise/cmq_tls.c`
- Cache common config results in cmq_tls_config_t.
- RED: `tests/test_tls_session_init.c::second_init_faster`.

### P3 5.0 fan-out wildcard puback
- File: `src/enterprise/cmq_mqtt_server.c`
- Add `+` wildcard match support.
- RED: `tests/test_mqtt_listen.c::wildcard_subscribe_match`.

## 5. Gating

- 71 + new RED tests. Must stay green.
- Bench: msg_per_sec ≥ 25k, p99 ≤ 200µs.
- ASAN clean on close-by-fd + freelist.
- TSAN clean on rwlock fairness + multi-threaded accept.

## 7. Time Estimate

| Phase | Items | Serial |
|---|---|---|
| 0 | 2 small safety fixes | S: 2-3 days |
| 1 | rwlock (M) + freelist (S) + rate-limit (S) | M: 4-7 days |
| 2 | WS deflate (L) + multi-listener (M) + multi-accept (L) | XL: 12-20 days |
| 3 | 5 small polish | S: 2-3 days |
| **Total** | 14 P-items | **20-33 days** |

## 9. Stop

- RED test fails for wrong reason.
- Default ctest drops below 71.
- ASAN/TSAN reports.
- Bench regresses > 10%.
- Multi-threaded accept regresses single-conn throughput.
- Multi-listener bind fails on second listener.

**Lead handoff complete.**