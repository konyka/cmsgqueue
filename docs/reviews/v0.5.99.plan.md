# v0.5.99 — COMPRESSED on RESPONSE

## Goal

A zstd RESPONSE inflates before inbox decrement and
subject parse.

## Performance

Uncompressed RESPONSE: one extra flags compare at the
top of `handle_response`.

## Security

Same 16 MiB cap. SUBSCRIBE / CONNECT still rejected.
Failed inflate does not decrement inbox.

## Stop

- Unit tests green (new + existing reqc / parser).
- Push `v0.5.99` to `origin`.
