# v0.5.121 — reload persist_sync_interval_ms

## Goal

SIGHUP updates the live WAL periodic-fsync interval
without restarting.

## Performance

Off the PUBLISH path. One integer store per reload.
`maybe_fsync` still compares against the interval on
append; no extra work when idle.

## Security

Omitted 0 does not disable an active fsync policy.

## Stop

- Unit tests green (new + psyn).
- Push `v0.5.121` to `origin`.
