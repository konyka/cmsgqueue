# v0.5.51 — Emit the dead audit events

## Goal

Operators see `auth_ok` / `auth_fail` / `persist_*` /
`tls_handshake_fail` in the JSON audit stream.

## Performance

No extra work on admitted PUBLISH. Audit stays mutex +
best-effort file append, same as today.

## Security

No passwords in `details`. User/subject still JSON-escaped.

## Stop

- Unit tests green.
- Push `v0.5.51` to `origin`.
