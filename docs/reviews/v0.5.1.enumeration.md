# CMSGQueue v0.5.1 — Remaining Gap Enumeration

**Scope.** Enumerate unimplemented/deferred features vs. the v0.4.0 plan
(`docs/reviews/hyperplan-v040-plan.md`) and v0.5.0 bundle
(`docs/reviews/hyperplan-v050-bundle.md`), with file:line citations verified
against HEAD `cbc5cc9`. Enumeration only — no design, no critique.
Format: **gap → file:line → impact → effort**.

---

## P1 — F19 server-side MQTT listener (still a stub) — SPLIT P1a/P1b
- **gap** → `src/enterprise/cmq_mqtt_server.c:4-15` — `cmq_mqtt_server_listen()` returns 0 (comment: "deferred to v0.5.5"); `cmq_mqtt_server_start_listener()` is an empty stub → **impact**: no MQTT broker; plan F6 (QoS 0/1, CONNECT/PUBLISH/SUBSCRIBE/PING/DISCONNECT, wildcard, 6 tests, bench) entirely absent → **effort**: XL
- **P1a** (skeleton + CONNECT/PUBLISH QoS0/1/SUBSCRIBE/PING): the original v0.4.0 F6 scope → **effort**: L
- **P1b** (QoS 2 + 5.0 props): explicitly deferred; client lib has zero PUBACK/SUBACK/PINGRESP encoders (`src/enterprise/cmq_mqtt.c` defines no 0x40/0x90/0xD0 packet types) → **effort**: XL, defer to v0.5.x
- **gap** → `src/enterprise/cmq_mqtt_server.h:8-22` — header still documents "STUB", ENOSYS, deferral to v0.4.0 (now stale vs impl returning 0) → **impact**: misleading API contract → **effort**: XS
- **gap** → `src/server/cmq_server.c` — zero callers of `cmq_mqtt_server_listen`/`start_listener`; no `mqtt_port` config key → **impact**: listener never starts → **effort**: M (wiring) once stub filled
- **gap** → `tests/test_mqtt_listen.c:22-37` — both tests assert nothing (`(void)rc`); comments defer real protocol to v0.5.3 → **impact**: no regression coverage → **effort**: S (rewrite)
- **gap** → `docs/features/mqtt-server-stub.md` — still titled "STUB" → **impact**: doc/impl drift → **effort**: S
- **verification** → rewrite `tests/test_mqtt_listen.c`: `connect_connack`, `subscribe_suback`, `publish_forwarded`, `pingreq_pingresp`, `qos0_delivery`, `qos1_puback`, `wildcard_subscribe` (7 scenarios) + `tests/fuzz/fuzz_mqtt.c` → pass = 7/7 + fuzz runs clean.
## P2 — F18 persistent sublist: record wired but LOAD never called
- **gap** → `src/server/cmq_server.c:3565` (record_sub), `:3599` (record_unsub), `:6632` (open) wired — **but** `cmq_sublist_persist_load()` (impl `src/server/cmq_sublist_persist.c:91`) has **zero** callers in `src/` (only in `tests/test_sublist_persist.c`) → **impact**: subscriptions persist on write but are never restored on restart; recovery path (plan F5 step 3) missing → **effort**: M
- **gap** → `src/server/cmq_server.c:6637-6690` — F5 replay loop replays publishes only, not subscriptions → **impact**: no restart recovery of subs → **effort**: M
- **gap** → `src/server/cmq_sublist.c` — zero persist integration (grep 0 hits); mirror lives in `cmq_server.c`, not sublist.c as plan §F5 risk-note specified → **impact**: locking divergence risk → **effort**: S
- **gap** → persist module has no snapshot/compact; plan `snapshot_compact` test absent → **impact**: unbounded WAL growth → **effort**: M
- **gap (correctness)** → `handle_publish` at `cmq_server.c:2817` gates on `client_account_live`; the zeroed `replay_c` (`:6671-6681`) has `account_epoch=0` vs `$default` epoch=1 (`cmq_account.c:176`, `cmq_server.c:6567`) → every replayed record is rejected "account inactive" → **impact**: silent data-loss on restart (severity: CRITICAL) → **effort**: S to fix
- **verification** → new end-to-end `test_sublist_persist_wire.server_restart_restores_sub` (subscribe → restart server with persist_dir → sub restored + publish delivered) + `unsubscribe_removes_on_restart` → pass = publish delivered after restart; `cmq_sublist_persist_load` invoked in `cmq_server_create` (grep-verifiable).
## P3 — F17 inter-node TLS: sess wrapper exists, never created in route.c
- **gap** → `src/cluster/cmq_route.c:174-192` `write_one`/`read_one` branch on a `sess` param, but the only call sites pass `NULL` (`:102`, `:156`) → **impact**: routes always plaintext → **effort**: M
- **gap** → no `cmq_route_tls_sess_create` call anywhere in `src/`; `route_require_tls` config unread → **impact**: inter-node encryption dead code → **effort**: M
- **gap** → `src/cluster/cmq_route_tls.h:16-20` explicitly "socket BIO-wrap queued for v0.5.0" — unimplemented → **impact**: matches plan F4 tests 2-5 (cleartext-reject, mTLS, handshake-fail, throughput) absent → **effort**: L
- **gap** → `tests/test_route_tls.c` / `test_route_tls_sess.c` test NULL/basic surface only → **impact**: no negotiation coverage → **effort**: S
- **verification** → `tests/test_route_tls.c`: `cleartext_disabled_when_configured`, `handshake_ok`, `handshake_fail_bad_cert`; `tests/test_route_tls_sess.c`: `echo_over_tls` (real-fd SSL round-trip) → pass = 4/4; `route_require_tls` config key wired (grep).
## P4 — F12 TLS: per-listener SSL_CTX array not implemented
- **gap** → `src/enterprise/cmq_tls.c:45` single `SSL_CTX *ssl_ctx` per config (comment says "per-listener" but there is exactly one) vs. plan §2.1 `SSL_CTX **` array → **impact**: cannot serve distinct certs/ciphers per listener → **effort**: M
- **gap** → `src/server/cmq_server.h:162` single `listen_fd`, `:195` single `tls_config` — no multi-listener server support → **impact**: multi-listener model absent → **effort**: M
- **note** → F12 core IS present: ALPN (`cmq_tls.c:225`), cert reload (`:249`), mTLS `SSL_VERIFY_PEER` (`:293-296`) → only the per-listener CTX model is missing.
- **seq** → must land AFTER P5 (both touch `cmq_tls.c` `tls_build_ssl_ctx`) to avoid double re-touch.
- **verification** → `tests/test_tls_per_listener.c`: `two_listeners_distinct_ctx`, `alpn_negotiation`, `cert_reload_no_disconnect` → pass = 3/3; server struct exposes per-listener `SSL_CTX **` (grep).
## P5 — mTLS client cert chain validation (custom CRL/OCSP) — OPEN
- **gap** → `src/enterprise/cmq_tls.c:293-296` sets `SSL_VERIFY_PEER` but no custom CRL/OCSP callback → **impact**: chain-revocation not enforced; v0.5.0 bundle item 8 open; CHANGELOG defers to v0.5.1+ → **effort**: M
- **gap (correctness)** → `tls_build_ssl_ctx` (`cmq_tls.c:95-140`) loads CA (`:138`) but never calls `SSL_CTX_set_verify`; the only verify call is in `cmq_tls_reload` (`:293-296`), which the server never calls (grep: only definition); `cmq_tls_set_verify`/`set_ca` are test-only → **impact**: mTLS is dead in production; no `tls_ca`/`tls_verify_peer` config keys → **effort**: M
- **verification** → `tests/test_mtls_api.c`: `ca_list_set`, `chain_rejects_unknown_ca`, `crl_path_accepted`, `verify_peer_off_admits` → pass = 4/4; `tls_build_ssl_ctx` calls `SSL_CTX_set_verify` when `verify_peer` (grep).
## P6 — WAL replay parallelization — OPEN
- **gap** → `src/server/cmq_server.c:6647` `for (seq=1; seq<=last; seq++)` single-threaded O(N) → **impact**: slow restart at scale; CHANGELOG defers to v0.5.1+ → **effort**: M
- **verification** → `tests/test_persist_unit.c`: `replay_parallel_consistent`, `replay_deterministic`, `replay_partition_isolated` → pass = 3/3 + ≤10 ms/100K subs. NOTE: predicated on P2 replay no-op being fixed first (else RED for wrong reason).
## P7 — test_stress flake: mitigated, barrier/10-run test missing
- **gap** → `tests/test_stress.c:175` has `wait_ms(200)` settle (`:210`) + per-sub drain (`:239-264`) but **no** `pthread_barrier_t` and **no** `flake_regression_10_runs` (grep 0 hits) → **impact**: plan F1 verification gate (10/10) not codified → **effort**: S
- **verification** → add `flake_regression_10_runs` to `tests/test_stress.c` → pass = 10/10 consecutive runs.
## P8 — Docs / ADR / bench / fuzz gaps (deferred deliverables)
- **gap** → no `bench/` dir (plan: bench_tls_handshake, bench_route_tls, bench_sublist_persist, bench_mqtt_listen) → **impact**: perf budgets untested → **effort**: M
- **gap** → only ADR `docs/adr/0001-build-hardening.md`; plan's 0010-0013 absent → **impact**: design decisions undocumented → **effort**: S
- **gap** → stub-titled docs remain: `docs/features/mqtt-server-stub.md`, `sublist-persist.md`, and both `tls-hardening.md` + `tls-openssl.md` coexist (plan wanted rewrite) → **impact**: doc/impl drift → **effort**: S
- **gap** → no MQTT fuzz harness; `tests/fuzz/` has only `fuzz_parser.c` → **impact**: plan's "new MQTT fuzz harness" absent → **effort**: S
- **verification** → `bench/` builds (bench_tls_handshake, bench_route_tls, bench_sublist_persist, bench_mqtt_listen); ADRs 0010-0013 present → pass = bench compiles + perf budgets met.
---

## VERIFIED IMPLEMENTED (not gaps)
- F14/F15/F16 wire-up: `cmq_acl_check` `cmq_server.c:2952`, `cmq_quota_check_publish` `:2959`, `cmq_blocklist_check` `:4435`. ✔
- F7 hardening: HSTS `:5104-5105`, audit rotation `cmq_audit.c:86-91`, drain timeout `cmq_server.c:7278-7307`. ✔
- N1 per-subject rate limit `:2967-2973`. ✔ N2 hot reload. ✔ N3 audit test. ✔

## Priority summary (revised after cross-attack — correctness first)
**P2 (CRITICAL, S) → P5 (M) → P3 (L) → P1 (L/XL) → P4 (M, after P5) → P6 (M, after P2) → P7 (S) → P8 (S/M).**
- P1 as a whole ships before P4 (per lead). Within P1, schedule P1a (skeleton + CONNECT/PUBLISH QoS0/1/SUBSCRIBE/PING) first; P1b (QoS 2 + 5.0 props) is a deferred tail.
