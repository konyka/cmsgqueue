# v0.5.88 — Tombstone TTL + dirty compact

## Goal

Young tombstones can survive compact; a dirty sealed
segment can compact itself after rotate.

## Performance

Append: one compare after rotate when dirty den is 0.
TTL does not touch the live WAL.

## Security

den=0 disables auto-compact. Null handles fail closed.

## Stop

- Unit tests green (new + existing keycompact).
- Push `v0.5.88` to `origin`.
