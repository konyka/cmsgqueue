# v0.5.95 — Stream consume / ack

## Goal

REQUEST `$JS.<name>.<consumer>` pulls the next seq;
PUBLISH with an 8-byte BE seq acks it.

## Performance

`$JS.<name>` parse still rejects a dot (one strchr).
Consumer subjects are a second parse on the `$` path.

## Security

Same name alphabet as streams. Ack does not create a
stream. Consume does not advance the watermark.

## Stop

- Unit tests green (new + existing js / jsr).
- Push `v0.5.95` to `origin`.
