# v0.5.86 — Leaf/gateway CONNECT/CONNACK e2e

## Scope
`cmq_leaf_connect` and `cmq_gateway_connect_remote`
already dial + `cmq_peer_handshake`. `test_cluster.c`
never ran that path. This increment is a loopback e2e
of the production handshake.

## Design (performance-first, safety, reliability)

- Hub stub: accept, read CONNECT, write CONNACK 0.
- Leaf and gateway use the real connect APIs.
- CONNACK code != 0 fails closed.
- No extra I/O on PUBLISH.

## TDD
1. **Red**: leaf connect, gateway connect, bad CONNACK,
   reject.
2. **Green**: loopback hub stub + existing handshake.
3. **Verify**: `test_cluster.c` still compiles.

## Files
- `tests/test_leafe.c`
- `docs/features` cluster notes if present
- CHANGELOG, this file, plan, benches
