# v0.5.126 — reload MQTT bridge maps

## Goal

SIGHUP replaces the live CMQ→MQTT subject maps without
reconnecting the upstream broker.

## Performance

Off the PUBLISH path. One lock + ≤8 struct copies.

## Security

Bad qos / empty topic fails closed. Omitted maps do not
wipe a working table.

## Stop

- Unit tests green (new).
- Push `v0.5.126` to `origin`.
