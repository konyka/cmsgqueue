# v0.5.76 — remote JWKS fetch

## Goal

`jwks_url` loads a JWKS document over HTTP so CONNECT `kid`
lookup uses the same cache as `jwks_json`.

## Performance

One GET at create. CONNECT still does ≤8 kid compares.

## Security

Loopback-safe URL rules match OTLP. Oversize / non-2xx /
bad JSON fail closed. HTTPS stays deferred.

## Stop

- Unit tests green (new + jwks).
- Push `v0.5.76` to `origin`.
