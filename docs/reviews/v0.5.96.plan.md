# v0.5.96 — COMPRESSED on PUBLISH

## Goal

A zstd PUBLISH inflates before routing so subscribers
see plaintext, not compressed garbage.

## Performance

Uncompressed path: one extra `flags & COMPRESSED` compare
at the top of `handle_publish`.

## Security

Same 16 MiB content-size cap as BATCH. MESSAGE still
rejected so inbound MESSAGE cannot smuggle a bomb.

## Stop

- Unit tests green (new + existing compress / parser).
- Push `v0.5.96` to `origin`.
