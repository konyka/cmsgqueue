# v0.5.100 — Request spans

## Goal

A successful local REQUEST answer queues `KIND_REQUEST`
with the accept trace id.

## Performance

One extra offer on the REQUEST success path only.
Unanswered REQUEST does not touch the ring.

## Security

No subject, reply-to, or payload on the span.

## Stop

- Unit tests green (new + existing otel).
- Push `v0.5.100` to `origin`.
