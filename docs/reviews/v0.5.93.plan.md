# v0.5.93 — Stream PUBLISH path

## Goal

PUBLISH `$JS.<name>` appends to a named in-memory stream.

## Performance

Non-`$` subjects unchanged. `$JS.` is after the existing
one-byte `$` gate.

## Security

Names are `[A-Za-z0-9_-]`. Persist dir is `{persist_dir}/js`
(no `..`). Bodies stay off the cursor file.

## Stop

- Unit tests green (new + existing stream).
- Push `v0.5.93` to `origin`.
