# v0.5.157 — attach subscription persist on reload

## Goal

SIGHUP opens `cmq-subs.wal` when create left persist
NULL. Empty / omitted keeps off. An existing handle
is not remounted. `..` / `\` fail closed.

## Performance

Existing handle: one pointer check, no open.
First attach: one `fopen` of `cmq-subs.wal`.

## Security

`..` / `\` / controls fail closed. Open miss does
not rewrite live `persist_dir`.

## Stop

- Unit tests green.
- Push `v0.5.157` to `origin`.
