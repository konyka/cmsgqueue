# v0.5.47 — connz / subz / routez

## Goal

Operators can scrape per-conn / per-sub / per-route state as
JSON on the existing HTTP dispatcher.

## Performance

- Not on the PUBLISH path.
- Snapshot is a bounded copy; format is stack-only.

## Security

- JSON-escape untrusted strings.
- No credentials in the body.

## Stop

- Unit tests green.
- Push `v0.5.47` to `origin`.
