# CMSGQueue v0.5.18 — Work Breakdown Structure

**Basis.** v0.5.18 enumeration (10 P-items). HEAD v0.5.17 (commit
469384a, 80/80 tests, ~32.6K msg/s).

## 1. Phases

| Phase | Items | Exit condition |
|---|---|---|
| **0: Test prep** | P3 atomic 32-bit; P3 multi-listener test | Test infra ready |
| **1: Performance + feature** | P1 multi-listener runtime (with port-guard); P2 TLS regression; P2 mqtt bridge freelist verify; P3 TLS session_init cache; P3 mqtt bridge alloc unify; P3 multi-threaded accept throughput test | Multi-listener works; TLS regression test catches UAF; bridge alloc optimized |
| **2: WS compression** | P1 WS permessage-deflate; P2 WS deflate implementation | WS compresses; bandwidth saved |
| **3: Close-out** | CHANGELOG + bench + tag + push | v0.5.18 shipped |

## 4. Per-P Micro-Plans

### P3 atomic 32-bit CI test
- File: `tests/test_atomic_32.c` (no-op smoke test)
- Add a simple compile-time assert.
- RED: builds under -m32.

### P3 multi-listener test
- File: `tests/test_multi_listen.c` (new)
- Configure cmq_server with 2 listeners; bind both.
- RED: connect to both ports.

### P1 multi-listener runtime (with port-guard)
- File: `src/server/cmq_server.c:7027`
- Iterate over `tls_config_slots[]`; bind each slot's port.
- Port-guard: skip ports in `18800-18999` (test_server range).
- RED: multi-listener test passes.

### P2 TLS regression test (real)
- File: `tests/test_tls_reload_safe.c` (add session test)
- Real session + reload.
- RED: no ASAN error.

### P2 mqtt bridge freelist verification (under load)
- File: `src/enterprise/cmq_mqtt_server.c` (stress test)
- 1000 PUBLISH cycle; cap not exceeded.
- RED: bounded.

### P3 TLS session_init cache (real, beyond smoke)
- File: `src/enterprise/cmq_tls.c` (real cache)
- Per-size-class freelist for ciphers/alpn.
- RED: 1000 inits < 100ms.

### P3 mqtt bridge allocation unification (real)
- File: `src/enterprise/cmq_mqtt_server.c` (combined alloc)
- Single allocation per payload.
- RED: freelist test verifies.

### P3 Multi-threaded accept loop throughput test
- File: `tests/test_multi_thread_accept.c` (add throughput assertion)
- 1 vs 2 acceptors; assert 2x throughput.
- RED: 2 acceptors faster.

### P1 WS permessage-deflate (real)
- File: `src/server/cmq_ws.c`
- Negotiate `permessage-deflate; server_no_context_takeover`.
- Add z_stream per connection.
- RED: WS deflate test asserts compression ratio.

### P2 WS deflate implementation (real)
- File: `src/server/cmq_ws.c`
- Add zlib inflate/deflate around ws_write_all/ws_read_all.
- RED: WS deflate test asserts compress ratio.

## 5. Gating

- 80 + new RED tests. Must stay green.
- Bench: msg_per_sec ≥ 25k, p99 ≤ 200µs.
- ASAN clean on TLS regression + multi-listener.

## 7. Time Estimate

| Phase | Items | Serial |
|---|---|---|
| 0 | 2 prep | XS: 1 day |
| 1 | 6 functional | M: 5-8 days |
| 2 | 2 WS compression | L: 5-9 days |
| 3 | close-out | XS: 0.5 day |
| **Total** | 10 P-items | **12-19 days** |

## 9. Stop

- RED test fails for wrong reason.
- Default ctest drops below 80.
- ASAN/TSAN reports.
- Bench regresses > 10%.
- test_server regression must NOT recur.

**Lead handoff complete.**