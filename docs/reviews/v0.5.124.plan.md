# v0.5.124 — reload F14 quota / N1 subject RL

## Goal

SIGHUP updates per-account quota and per-subject rate
limits without restarting or replacing the live tables.

## Performance

Off the PUBLISH path. A few integer stores per reload.
Check still uses the same slot table.

## Security

Omitted 0 does not disable a working cap. Oversize values
fail closed. In-place update avoids reload UAF.

## Stop

- Unit tests green (new + quota).
- Push `v0.5.124` to `origin`.
