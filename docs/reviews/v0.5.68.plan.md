# v0.5.68 — Object-store PUBLISH path

## Goal

PUBLISH to `$OBJ.<name>` writes or deletes a named blob.

## Performance

One `'$'` check shared with `$KV.`. No store: no extra work.

## Security

Same name rules as the library. Path traversal rejected.

## Stop

- Unit tests green (new + obj).
- Push `v0.5.68` to `origin`.
