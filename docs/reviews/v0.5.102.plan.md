# v0.5.102 — Disconnect spans

## Goal

A graceful inbound DISCONNECT queues `KIND_DISCONNECT`
with the accept trace id.

## Performance

One extra offer on the DISCONNECT opcode only.
TCP drop / idle timeout do not touch the ring.

## Security

No subject, user, or payload on the span.

## Stop

- Unit tests green (new + existing otel).
- Push `v0.5.102` to `origin`.
