# v0.5.122 — reload live rate / timeout scalars

## Goal

SIGHUP updates connect-rate, inbox pending, ping, and
write-timeout caps without restart.

## Performance

Off the PUBLISH path. Four integer stores per reload.

## Security

Omitted 0 does not disable an active cap. Out-of-range
fails closed.

## Stop

- Unit tests green (new + ath).
- Push `v0.5.122` to `origin`.
