# v0.5.98 — COMPRESSED on REQUEST

## Goal

A zstd REQUEST inflates before inbox/subject parse.

## Performance

Uncompressed REQUEST: one extra flags compare at the
top of `handle_request`.

## Security

Same 16 MiB cap. RESPONSE / CONNECT still rejected.

## Stop

- Unit tests green (new + existing msgc / parser).
- Push `v0.5.98` to `origin`.
