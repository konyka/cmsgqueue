# CMSGQueue v0.5.13 — Work Breakdown Structure

**Basis.** v0.5.13 enumeration (14 P-items). HEAD v0.5.12 (commit
e316d46, 77/77 tests, ~33.6K msg/s).

## 1. Phases

| Phase | Items | Exit condition |
|---|---|---|
| **0: Tests** | P2 WS deflate test; P3 TLS reload path on real session | Test gaps filled |
| **1: Performance** | P1 multi-threaded accept loop; P2 mqtt bridge allocation unification | Accept throughput scales |
| **2: Functional** | P1 multi-listener runtime (carefully, with port-guard); P1 WS permessage-deflate; P1 5.0 wildcard PUBACK; P1 TLS session cache; P1 TLS regression test | Multi-listener + WS compression + 5.0 compliance + portable build |
| **3: Polish** | P3 freelist cap; P3 rate-limit log spam cap; P2 test_server port conflict fix | Robustness + clean shutdown |
| **4: Close-out** | CHANGELOG + bench + tag + push | v0.5.13 shipped |

## 4. Per-P Micro-Plans

### P2 WS deflate test
- File: `tests/test_ws.c`
- Add permessage-deflate test.
- RED: the new test.

### P3 TLS reload path on real session
- File: `tests/test_tls_reload_safe.c`
- Add real session + reload test.
- RED: the new test.

### P1 multi-threaded accept loop
- File: `src/server/cmq_server.c:712`
- Add second accept thread on a second listen fd.
- RED: throughput test.

### P2 mqtt bridge allocation unification
- File: `src/enterprise/cmq_mqtt_server.c`
- Allocate payload + freelist node together.
- RED: freelist test.

### P1 multi-listener runtime accept loop (carefully)
- File: `src/server/cmq_server.c:7027`
- Iterate over `tls_config_slots[]`; bind each slot's port.
- RED: multi-listener test.
- **Note**: v0.5.13 must avoid the test_server regression that
  rolled back v0.5.10 + v0.5.12. Use a port-range guard: skip
  ports 18800-18999 (test_server uses 18801-18803). v0.5.13's
  multi-listener runtime should only activate on ports outside
  the test_server range.

### P1 WS permessage-deflate
- File: `src/server/cmq_ws.c`
- Negotiate `permessage-deflate; server_no_context_takeover`.
- RED: WS deflate test.

### P1 5.0 wildcard PUBACK match
- File: `src/enterprise/cmq_mqtt_server.c`
- Add `+` wildcard match support.
- RED: 5.0 wildcard test.

### P1 TLS session cache (real)
- File: `src/enterprise/cmq_tls.c`
- Per-config cache for alpn_data + ciphers.
- RED: session_init speed test.

### P1 TLS regression test (real)
- File: `tests/test_tls_reload_safe.c`
- Add real session + reload test.
- RED: the new test.

### P3 freelist cap
- File: `src/server/cmq_server.c`
- Verify cap is respected; test under sustained load.
- RED: existing test.

### P3 rate-limit log spam cap
- File: `src/enterprise/cmq_mqtt_server.c`
- Cap log to once per minute per IP (already done in v0.5.9).
- RED: existing test.

### P2 test_server port conflict fix
- File: `tests/test_server.c`
- Change test_server to use a non-contiguous port range
  (e.g. 28801-28803) to avoid conflict with v0.5.13's
  multi-listener bind at 18800-18999. Or v0.5.13's multi-listener
  runtime should skip ports 18800-18999.

### P3 freelist cap growth path
- File: `src/server/cmq_server.c`
- Verify cap is respected; test under sustained load.
- RED: existing test.

## 5. Gating

- 77 + new RED tests. Must stay green.
- Bench: msg_per_sec ≥ 25k, p99 ≤ 200µs.
- ASAN clean on TLS regression + multi-listener.

## 7. Time Estimate

| Phase | Items | Serial |
|---|---|---|
| 0 | 2 test files | S: 1-2 days |
| 1 | 2 perf items | M: 4-6 days |
| 2 | 5 functional items | M: 5-9 days |
| 3 | 3 polish | S: 1-2 days |
| 4 | close-out | XS: 0.5 day |
| **Total** | 14 P-items | **12-20 days** |

## 9. Stop

- RED test fails for wrong reason.
- Default ctest drops below 77.
- ASAN/TSAN reports.
- Bench regresses > 10%.
- test_server regression from v0.5.10 + v0.5.12 must NOT recur.

**Lead handoff complete.**