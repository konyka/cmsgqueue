# v0.5.65 — JWKS oct-key cache

## Goal

CONNECT JWTs with `kid` verify against a configured JWKS
oct-key set.

## Performance

Parse off accept. Lookup is a linear scan of at most 8 keys.

## Security

Verify-only. Unknown kid fails. No remote fetch this cut.

## Stop

- Unit tests green (new + jwt).
- Push `v0.5.65` to `origin`.
