# v0.5.134 — reload jwks_ca

## Goal

SIGHUP applies `jwks_ca` to a running JWKS sidecar.
The next refresh uses the new PEM. URL fetch stays
create-time.

## Performance

One mutex + memcpy of a path. GET cadence unchanged.

## Security

`..` fails closed. Empty/omitted cannot wipe a working
CA. Snapshot under lock avoids a torn path mid-GET.

## Stop

- Unit tests green.
- Push `v0.5.134` to `origin`.
