# v0.5.133 — empty log_file / slot-0 TLS store NULL

## Goal

`log_file =` and `tls_cert =` / `tls_key =` disable those
strings. Create does not `fopen("")`.

## Performance

One path walk at load. No hot-path change.

## Security

`..` / `\` / controls in `log_file` fail closed. Auth
empty strings still reject at validate.

## Stop

- Unit tests green.
- Push `v0.5.133` to `origin`.
