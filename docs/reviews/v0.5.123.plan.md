# v0.5.123 — reload payload / sub / client caps

## Goal

SIGHUP updates `max_payload_size`, `max_subs_per_client`,
and `max_clients` without restart.

## Performance

Off the PUBLISH path. Three integer stores per reload.

## Security

Omitted 0 does not raise or drop a working cap.
Oversize values fail closed.

## Stop

- Unit tests green (new + lim).
- Push `v0.5.123` to `origin`.
