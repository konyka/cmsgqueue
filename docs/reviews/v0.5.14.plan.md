# CMSGQueue v0.5.14 — Work Breakdown Structure

**Basis.** v0.5.14 enumeration (13 P-items). HEAD v0.5.13 (commit 88736f4,
77/77 tests, ~33.6K msg/s).

## 1. Phases

| Phase | Items | Exit condition |
|---|---|---|
| **0: Critical test prep** | P2 test_server port fix (18801→28801) | Port conflict resolved |
| **1: Performance** | P1 multi-threaded accept loop; P2 mqtt bridge allocation unification | Accept throughput scales |
| **2: Functional** | P1 multi-listener runtime (with port guard); P1 WS permessage-deflate; P1 5.0 wildcard PUBACK; P1 TLS session cache; P1 TLS regression test | Multi-listener + WS compression + 5.0 compliance + portable build |
| **3: Polish** | P3 freelist cap; P3 rate-limit log spam cap | Robustness + clean shutdown |
| **4: Close-out** | CHANGELOG + bench + tag + push | v0.5.14 shipped |

## 4. Per-P Micro-Plans

### P2 test_server port fix (critical first step)
- File: `tests/test_server.c`
- Change ports 18801-18803 → 28801-28803.
- RED: all test_server tests still pass.

### P1 multi-threaded accept loop
- File: `src/server/cmq_server.c:712`
- Add second accept thread on a second listen fd.
- RED: throughput test.

### P2 mqtt bridge allocation unification
- File: `src/enterprise/cmq_mqtt_server.c`
- Allocate payload + freelist node together.
- RED: freelist test.

### P1 multi-listener runtime accept loop (with port guard)
- File: `src/server/cmq_server.c:7027`
- Iterate over `tls_config_slots[]`; bind each slot's port.
- Skip ports 28800-28999 (test_server range 28801-28803).
- Skip ports 18800-18999 (avoid legacy test range).
- RED: multi-listener test.

### P1 WS permessage-deflate
- File: `src/server/cmq_ws.c`
- Negotiate `permessage-deflate; server_no_context_takeover`.
- RED: WS deflate test.

### P1 5.0 wildcard PUBACK match
- File: `src/enterprise/cmq_mqtt_server.c`
- Add `+` wildcard match support.
- RED: 5.0 wildcard test.

### P1 TLS session cache (real)
- File: `src/enterprise/cmq_tls.c`
- Per-config cache for alpn_data + ciphers.
- RED: session_init speed test.

### P1 TLS regression test (real)
- File: `tests/test_tls_reload_safe.c`
- Add real session + reload test.
- RED: the new test.

### P3 freelist cap
- File: `src/server/cmq_server.c`
- Verify cap is respected.
- RED: existing test.

### P3 rate-limit log spam cap
- File: `src/enterprise/cmq_mqtt_server.c`
- Cap log to once per minute per IP.
- RED: existing test.

## 5. Gating

- 77 + new RED tests. Must stay green.
- Bench: msg_per_sec ≥ 25k, p99 ≤ 200µs.
- ASAN clean on TLS regression + multi-listener.

## 7. Time Estimate

| Phase | Items | Serial |
|---|---|---|
| 0 | 1 port fix | XS: 0.5 day |
| 1 | 2 perf items | M: 4-6 days |
| 2 | 5 functional items | M: 5-9 days |
| 3 | 2 polish | XS: 1 day |
| 4 | close-out | XS: 0.5 day |
| **Total** | 10 P-items | **11-17 days** |

## 9. Stop

- RED test fails for wrong reason.
- Default ctest drops below 77.
- ASAN/TSAN reports.
- Bench regresses > 10%.
- test_server regression from v0.5.10 + v0.5.12 must NOT recur.

**Lead handoff complete.**