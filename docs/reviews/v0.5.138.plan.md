# v0.5.138 — reload otlp_endpoint

## Goal

SIGHUP applies `otlp_endpoint` to a running OTLP exporter.
The next export uses the new host. Start stays create-time.

## Performance

One parse + struct copy. Export cadence unchanged.

## Security

Bad scheme / host / path (`..`) fail closed. Empty/omitted
cannot wipe a working URL. CA is preserved across URL apply.

## Stop

- Unit tests green.
- Push `v0.5.138` to `origin`.
