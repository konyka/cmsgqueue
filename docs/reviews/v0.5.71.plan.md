# v0.5.71 — HPACK Huffman

## Goal

Huffman-coded HPACK strings decode (and encode) per RFC 7541.

## Performance

No heap. Encode is table lookup + bit pack.

## Security

Oversize output, truncated input, and illegal padding fail closed.

## Stop

- Unit tests green (new + hpack).
- Push `v0.5.71` to `origin`.
