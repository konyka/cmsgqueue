# v0.5.119 — reload auth / JWT / nkey

## Goal

SIGHUP / `cmq_server_reload` rotates CONNECT credentials
without restarting the process.

## Performance

Off the PUBLISH path. At most eight `strdup`s per reload.

## Security

Empty/omitted keys do not wipe a working secret.
`jwt_leeway_sec` outside 0–3600 fails closed.

## Stop

- Unit tests green (new + rld + tlr).
- Push `v0.5.119` to `origin`.
