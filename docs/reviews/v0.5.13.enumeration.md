# CMSGQueue v0.5.13 — Remaining Gap Enumeration

**Scope.** Items deferred from v0.5.12 (which shipped 2 verification
tests) plus new gaps at HEAD (v0.5.12, commit e316d46, 77/77 tests).

## Sources

- `docs/reviews/v0.5.12.enumeration.md` — 14-item catalog.
- `docs/reviews/v0.5.11.enumeration.md` — 14-item catalog.
- v0.5.12 ultrabrain review.

## P1 — Multi-listener runtime accept loop (carefully)

- **gap** → `src/server/cmq_server.c:7027, 7096` (cmq_server_run
  binds only `listen_fds[0]`; v0.5.10 + v0.5.12 both rolled back due
  to test_server port conflict). v0.5.13 must use a proper
  port-guard approach (e.g. detect test_server's specific port
  range 18801-18999 and skip those ports).
- **impact**: cannot serve two distinct certs/ports from one process.
- **effort**: M (200 LOC: iterate slots, per-listener accept_cb;
  guard test_server's port range).

## P1 — Multi-threaded accept loop

- **gap** → `src/server/cmq_server.c:712, 7069` (single listen fd
  registered with the ev_loop; accept_cb runs on the ev_loop
  thread). High connect rates bottleneck.
- **impact**: 100K+ conn/sec can't be served.
- **effort**: L (multi-threaded accept + per-conn handoff).

## P1 — WS permessage-deflate

- **gap** → `src/server/cmq_ws.c` (extend WS handshake to negotiate
  `permessage-deflate; server_no_context_takeover`; per-conn z_stream).
- **impact**: bandwidth waste on WS clients.
- **effort**: L.

## P1 — 5.0 wildcard PUBACK match

- **gap** → `src/enterprise/cmq_mqtt_server.c` (no `+` wildcard
  match support in PUBLISH delivery).
- **impact**: 5.0 compliance gap.
- **effort**: M.

## P1 — TLS session cache (real)

- **gap** → `src/enterprise/cmq_tls.c` (per-config cache for
  alpn_data + ciphers; v0.5.7 confirmed no per-session duplicate
  work but the ciphers/alpn cache could be even more effective).
- **impact**: low.
- **effort**: S.

## P1 — TLS regression test (real)

- **gap** → `tests/test_tls_reload_safe.c` (v0.5.8 smoke test;
  v0.5.12 added up_ref+free roundtrip; a real session + reload
  test would catch UAF regressions).
- **impact**: test gap.
- **effort**: S.

## P2 — mqtt bridge allocation unification

- **gap** → `src/enterprise/cmq_mqtt_server.c` (v0.5.8 added
  freelist but payload + freelist node are separate allocations;
  combining them saves a malloc).
- **impact**: small perf win.
- **effort**: S.

## P2 — WS deflate test

- **gap** → `tests/test_ws.c` (no test).
- **impact**: test gap.
- **effort**: S.

## P2 — test_server port conflict fix

- **gap** → `tests/test_server.c:108,142,180` (test_server uses
  ports 18801-18803; v0.5.10 + v0.5.12 attempts to bind additional
  ports 18802-18899 conflicted with these; v0.5.13 should either
  change test_server to use a non-contiguous range or skip those
  ports in the multi-bind).
- **impact**: test regression.
- **effort**: S.

## P3 — Freelist overflow cap

- **gap** → `src/server/cmq_server.c` (v0.5.7 capped at 64; v0.5.8
  mqtt bridge at 64; pathological mqtt_bridge could push past
  256 entries — the queue is bounded but payloads aren't).
- **impact**: bounded by queue size.
- **effort**: S.

## P3 — Rate-limit log spam cap

- **gap** → `src/enterprise/cmq_mqtt_server.c` (v0.5.9 rate-limit
  log per IP per minute; under hostile load with many IPs, log
  still appears per-minute per-IP).
- **impact**: log growth.
- **effort**: XS.

## P3 — Test cleanup

- **gap** → `tests/test_server.c:108` (test_server uses port 18801;
  v0.5.13 multi-listener attempt should skip ports 18800-18999
  to avoid the regression).
- **impact**: test isolation.
- **effort**: S.

## P3 — mqtt bridge freelist in one allocation

- **gap** → `src/enterprise/cmq_mqtt_server.c` (v0.5.8 freelist
  + separate payload alloc; v0.5.13 could combine them).
- **impact**: small perf win.
- **effort**: S.

## P3 — TLS reload path on real session

- **gap** → `tests/test_tls_reload_safe.c` (the v0.5.12 up_ref+free
  test doesn't actually exercise a reload during a session).
- **impact**: test gap.
- **effort**: S.

---

## Priority order

1. **P1** Multi-listener runtime accept loop (carefully, v0.5.13 with port-guard)
2. **P1** Multi-threaded accept loop
3. **P1** WS permessage-deflate
4. **P1** 5.0 wildcard PUBACK match
5. **P1** TLS session cache (real)
6. **P1** TLS regression test (real)
7. **P2** mqtt bridge allocation unification
8. **P2** WS deflate test
9. **P2** test_server port conflict fix
10. **P3** Freelist overflow cap
11. **P3** Rate-limit log spam cap
12. **P3** Test cleanup
13. **P3** mqtt bridge freelist in one allocation
14. **P3** TLS reload path on real session

## Verdict

v0.5.13 must focus on:
- **Performance**: #2 multi-threaded accept (bench could double)
- **Functional**: #1 multi-listener (carefully, with port-guard),
  #3 WS deflate, #4 5.0 wildcard
- **Reliability**: #6 TLS regression test (real)

Multi-listener (#1) is the highest-value win since the data
structure has been in place since v0.5.2 — but v0.5.13 must
carefully avoid the test_server regression that rolled back
v0.5.10 + v0.5.12. v0.5.13 should use a port-range guard (e.g.
detect test_server's 18800-18999 range and skip those ports).