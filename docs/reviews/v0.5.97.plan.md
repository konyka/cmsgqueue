# v0.5.97 — COMPRESSED on MESSAGE

## Goal

A zstd MESSAGE inflates and republishes so local
subscribers see plaintext.

## Performance

Uncompressed MESSAGE is still the default-unknown path.
Compressed MESSAGE pays inflate + one convert + publish.

## Security

Same 16 MiB cap. REQUEST still rejected. Convert fails
closed on a truncated MESSAGE body.

## Stop

- Unit tests green (new + existing pubc / parser).
- Push `v0.5.97` to `origin`.
