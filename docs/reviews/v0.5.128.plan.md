# v0.5.128 — omitted log_to_stdout defaults to 1

## Goal

A `cmq.conf` that omits `log_to_stdout` gets stdout
logging, matching `cmq.h`.

## Performance

One integer store at load. No hot-path change.

## Security

Explicit 0 / empty still disables stdout.

## Stop

- Unit tests green (new + config).
- Push `v0.5.128` to `origin`.
