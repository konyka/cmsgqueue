# v0.5.66 — HPACK static codec

## Goal

Library encode/decode of HPACK integers, literals, and
static indexed headers.

## Performance

No heap. Static table is const. Hot path unused until h2.

## Security

Integer DoS cap. Huffman rejected. No `h2` advertise.

## Stop

- Unit tests green.
- Push `v0.5.66` to `origin`.
