# CMSGQueue v0.5.5 — Work Breakdown Structure

**Basis.** v0.5.5 enumeration (11 P-items). HEAD v0.5.4 (70/70 tests, ~31K msg/s).

## 1. Phases

| Phase | Items | Exit condition |
|---|---|---|
| **0: Safety fixes** | P1 async WAL max payload cap; P1 sublist_insert no-leak on malloc failure; P1 MQTT PUBLISH rate limit; P1 bridge cleanup | OOM/DOS paths closed; bridge state bounded |
| **1: Performance follow-up** | P2 WS permessage-deflate; P2 rwlock fairness; P3 stat_messages_out split | Per-message-deflate opt-in; reader throughput stable under churn; metrics accurate |
| **2: Polish** | P3 TLS load partial cleanup; P3 close-by-fd; P3 accept loop single-threaded | Bad config doesn't leak; race windows closed; high-conn rates supported |
| **3: Close-out** | CHANGELOG + bench + tag + push | v0.5.5 shipped |

P2 Redis wire protocol and P3 JWT auth are XL; deferred to v0.6.

## 4. Per-P Micro-Plans

### P1 async WAL max payload size cap
- File: `src/store/cmq_filestore.c:760`
- Add `cmq_filestore_set_max_payload_size(fs, n)` + `static size_t max_payload_bytes`.
- cmq_filestore_async_enqueue rejects `len > max_payload_bytes` with -1.
- RED: `tests/test_wal_async.c::oversize_rejected_returns_minus1`.

### P1 sublist_insert no-leak on malloc failure
- File: `src/store/cmq_filestore.c:818`
- Move `copy = malloc(len)` BEFORE the head++/count++.
- RED: existing test_wal_async; assert count == 0 after simulated OOM.

### P1 MQTT PUBLISH rate limit
- File: `src/enterprise/cmq_mqtt_server.c:581`
- Hash by (source IP, account) → token bucket (capacity 100,
  refill 100/sec). New API `cmq_mqtt_set_rate_limit(cap, refill)`.
- RED: `tests/test_mqtt_listen.c::rate_limit_exceeded_returns_error`.

### P1 bridge cleanup on disconnect
- File: `src/enterprise/cmq_mqtt_server.c:96`
- Free g_mqtt_bridge_topics/payloads entries on mqtt_thread exit.
- RED: existing test_mqtt_bridge; assert no leaks under ASAN.

### P2 WS permessage-deflate
- File: `src/server/cmq_ws.c`
- Negotiate permessage-deflate in WS handshake; per-conn deflate
  state.
- RED: `tests/test_ws.c::permessage_deflate_compresses`.

### P2 rwlock fairness
- File: `src/server/cmq_sublist.c` + underlying `cmq_rwlock_t`.
- Replace with PTHREAD_RWLOCK_PREFER_WRITER_NONRECURSIVE_NP if
  available; otherwise mutex-protected reader-count.
- RED: `tests/test_sublist_concurrent.c::writer_does_not_starve_reader`.

### P3 TLS load partial cleanup
- File: `src/enterprise/cmq_tls.c:331`
- Add `goto fail` cleanup on each early-out path.
- RED: `tests/test_tls_api.c::bad_config_no_leak`.

### P3 close-by-fd
- File: `src/server/cmq_server.c`
- Confirm cmq_clients[] uses fd+generation; verify `close_cb` runs
  before fd reuse.
- RED: `tests/test_accept_race.c::close_then_rebind_safe`.

### P3 accept loop single-threaded
- File: `src/server/cmq_server.c:712`
- Add a second accept thread on a second listen fd; round-robin
  handoff to worker pool.
- RED: `tests/test_accept_throughput.c::two_acceptors_double_rate`.

### P3 stat_messages_out split
- File: `src/server/cmq_server.c:2191`
- Match the v0.5.3 P2 pattern: only tick for live (c->fd != -1).
- RED: existing test_wal_replay; assert messages_out == 0 after replay.

## 5. Gating

- 70 + new RED tests. Must stay green.
- Bench: msg_per_sec ≥ 25k, p99 ≤ 200µs, dropped ≤ 5000.
- ASAN clean on bridge cleanup + TLS load tests.
- TSAN clean on close-by-fd + rwlock fairness.

## 7. Time Estimate

Sizing: S = 0.5-1, M = 2-4, L = 4-7 days.

| Phase | Items | Serial |
|---|---|---|
| 0 | 4 small safety fixes | S: 2-4 days |
| 1 | WS deflate (L) + fairness (M) + stat out (XS) | M: 5-9 days |
| 2 | TLS (XS) + close-by-fd (S) + accept (L) | M: 5-9 days |
| 3 | CHANGELOG + tag | XS: 0.5 day |
| **Total** | 9 P-items | **13-23 days** |

## 9. Stop

- RED test fails for wrong reason.
- Default ctest drops below 70.
- Bench regresses > 10%.
- TLS load leaks under ASAN.
- Accept loop regresses single-conn throughput.
- Per-item fix lands before RED test.

**Lead handoff complete.**