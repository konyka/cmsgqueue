# v0.5.110 — `persist_sync_interval_ms` config-file key

## Goal

Operators can set the WAL periodic fsync interval from
`cmq.conf` without a programmatic assignment.

## Performance

One extra `strcmp` per config line. Hot-path append is
unchanged when the key is 0.

## Security

Negative and values above 86400000 fail closed.

## Stop

- Unit tests green (new + config / pdir).
- Push `v0.5.110` to `origin`.
