# v0.5.94 — Stream REQUEST-get

## Goal

REQUEST `$JS.<name>` returns the last payload to reply-to.

## Performance

Non-`$` REQUEST unchanged. `$JS.` is after the existing
one-byte `$` gate on the KV/object hook.

## Security

Same name alphabet as PUBLISH. No cursor mutation on get.

## Stop

- Unit tests green (new + existing js).
- Push `v0.5.94` to `origin`.
