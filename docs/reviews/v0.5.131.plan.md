# v0.5.131 — reload jwks_refresh_sec

## Goal

SIGHUP applies `jwks_refresh_sec` to a running JWKS
sidecar. URL fetch stays create-time.

## Performance

One atomic store. The sidecar still wakes every 1 s.

## Security

1–4 and >86400 fail closed. 0 / omitted cannot disable
or wipe a working interval.

## Stop

- Unit tests green.
- Push `v0.5.131` to `origin`.
