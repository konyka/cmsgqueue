# v0.5.70 — KV/object REQUEST-get

## Goal

REQUEST `$KV.<bucket>.<key>` or `$OBJ.<name>` returns the
stored value (or empty on miss) to reply-to.

## Performance

One `'$'` check. Other REQUEST paths unchanged.

## Security

Same name rules as PUBLISH. No reply-to is rejected.

## Stop

- Unit tests green.
- Push `v0.5.70` to `origin`.
